/*
 * @category  REG
 * @package   REG_ProductListingPage
 * @author    Wiktor Koscielny <wiktorkoscielny@gmail.com>
 */

import { Component } from "react";
import PropTypes from "prop-types";

/** @namespace REG/Component/PLP/Component */
export class ProductListingPageComponent extends Component {
  static propTypes = {
    // recipes: PropTypes.object || PropTypes.bool,
    // deleteSingleRecipe: PropTypes.func,
    // deleteAllRecipes: PropTypes.func,
    // countRecipes: PropTypes.func,
  };

  render() {
    return (
      <></>
    );
  }
}

export default ProductListingPageComponent;
